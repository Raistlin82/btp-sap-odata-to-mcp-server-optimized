_schema-version: "3.3.0"
ID: btp-sap-odata-to-mcp-server-optimized
description: "SAP OData to MCP Server Multi-target application"
version: 1.0.0

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm i

modules:
  - name: sap-mcp-server-optimized
    type: nodejs
    path: ./
    parameters:
      app-name: sap-mcp-server-optimized-${space}
      memory: 512M
      disk-quota: 1G
      buildpack: nodejs_buildpack
      command: npm run start
      keep-existing-routes: true
      health-check-type: http
      health-check-http-endpoint: /health
      timeout: 180
    properties:
      LOG_LEVEL: debug
      
    build-parameters:
      builder: custom
      commands:
        - npm run build
      ignore:
        - node_modules/
        - .git/
        - tests/
    requires:
      - name: sap-mcp-destination-optimized
      - name: sap-mcp-connectivity-optimized
      - name: sap-mcp-xsuaa-optimized
      - name: sap-mcp-logging-optimized

resources:
  # Destination Service
  - name: sap-mcp-destination-optimized
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite

  # Connectivity Service
  - name: sap-mcp-connectivity-optimized
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite

  # XSUAA Service
  - name: sap-mcp-xsuaa-optimized
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json

  # Application Logging Service
  - name: sap-mcp-logging-optimized
    type: org.cloudfoundry.managed-service
    parameters:
      service: application-logs
      service-plan: lite
